MONGO_URI=mongodb://127.0.0.1:27017/petitetheory
PORT=5000

JWT_SECRET=replace-with-a-long-random-string

# Used only by `npm run seed:admin` to provision/reset the admin login.
ADMIN_USERNAME=admin
ADMIN_PASSWORD=replace-with-a-strong-password
